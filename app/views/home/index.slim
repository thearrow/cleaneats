div[ng-app="cleanEats" ng-controller="cleanCtrl"]
  .row.text-center[ng-hide="isSearching"]
    h1
      | Clean Eats
    p
      | Make dining out with dietary restrictions easier
  .row.text-center.collapse.search
    .small-9.columns
      form[ng-submit="doTextSearch()"]
        input[type="text" placeholder="Address, City, Neighborhood, Zip..." ng-model="locationText"]
        i.icon-search
    .small-3.columns
      a.button.prefix[href="" ng-click="getLocation()"]
        i.icon-location-arrow
        |  Current Location
  .row.alert-holder
    div[ng-show="isLocationError" ng-cloak]
      .alert-box.alert[data-alert]
        | Couldn't get your location!
        a.close[href="#"]
          | ×
    div[ng-show="isLoading" ng-cloak]
      .alert-box[data-alert]
        | Loading Results....
  .row.text-center.map
    .large-9.columns
      #map[ng-show="isSearching" data-user="#{current_user}"]
        leaflet[width="100%" height=480 center="center" defaults="mapDefaults" markers="markers" events="events"]
        a.button.prefix[href="" ng-click="redoSearch()"]
          | Redo Search Here
    .large-3.columns